#include <stdio.h>
/**
 * main - A program that prints all single digit numbers
 *
 * Return: 0 (success)
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + 'c');
			c++;
		}
	putchar('\n');
	return (0);
}
